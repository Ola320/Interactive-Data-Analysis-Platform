<UserControl x:Class="DataAnalizer.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lucide="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#f8fafc">

    <UserControl.Resources>
        <!-- Styl dla pól tekstowych -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#f1f5f9"/>
            <Setter Property="BorderBrush" Value="#cbd5e1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernPasswordBox" TargetType="PasswordBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#f1f5f9"/>
            <Setter Property="BorderBrush" Value="#cbd5e1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Styl dla przycisków głównych -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#4f46e5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Styl dla linków/przełączników -->
        <Style x:Key="LinkButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#4f46e5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock Text="{TemplateBinding Content}" TextDecorations="Underline" VerticalAlignment="Center"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <!-- Główna karta formularza -->
        <Border Background="White" CornerRadius="16" Width="450" VerticalAlignment="Center" Margin="20" MaxHeight="560">
            <Border.Effect>
                <DropShadowEffect BlurRadius="30" Color="#0f172a" Opacity="0.08" ShadowDepth="5"/>
            </Border.Effect>

            <Grid Margin="30,25">
                <Grid.RowDefinitions>
                    <!-- Nagłówek trzyma stałą pozycję -->
                    <RowDefinition Height="Auto"/>
                    <!-- Zawartość formularza i status mogą się elastycznie skalować i przewijać -->
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Nagłówek i Logo -->
                <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,15">
                    <lucide:PackIconLucide Kind="Building2" Foreground="#4f46e5" Width="36" Height="36" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                    <TextBlock x:Name="TitleText" Text="Zaloguj się do platformy" FontSize="20" FontWeight="Bold" Foreground="#0f172a" HorizontalAlignment="Center"/>
                </StackPanel>

                <!-- ScrollViewer zabezpieczający przed ucinaniem dołu na mniejszych ekranach -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel>
                        <!-- KONTROLKA LOGOWANIA -->
                        <StackPanel x:Name="LoginPanel" Visibility="Visible">
                            <TextBlock Text="Nazwa użytkownika" Foreground="#475569" FontWeight="Medium" Margin="0,0,0,6"/>
                            <TextBox x:Name="UsernameBox" Style="{StaticResource ModernTextBox}" Margin="0,0,0,15"/>

                            <TextBlock Text="Hasło" Foreground="#475569" FontWeight="Medium" Margin="0,0,0,6"/>
                            <PasswordBox x:Name="PasswordBox" Style="{StaticResource ModernPasswordBox}" Margin="0,0,0,20"/>

                            <Button Content="Zaloguj się" Style="{StaticResource PrimaryButton}" Click="Login_Click" Margin="0,0,0,15"/>

                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="Nie masz konta? " Foreground="#64748b"/>
                                <Button Content="Zarejestruj się" Style="{StaticResource LinkButton}" Click="SwitchToRegister_Click"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- KONTROLKA REJESTRACJI (Zoptymalizowana przestrzenie pionowe) -->
                        <StackPanel x:Name="RegisterPanel" Visibility="Collapsed">
                            <TextBlock Text="Nazwa użytkownika" Foreground="#475569" FontWeight="Medium" Margin="0,0,0,4"/>
                            <TextBox x:Name="RegUsernameBox" Style="{StaticResource ModernTextBox}" Margin="0,0,0,10"/>

                            <TextBlock Text="Adres E-mail" Foreground="#475569" FontWeight="Medium" Margin="0,0,0,4"/>
                            <TextBox x:Name="RegEmailBox" Style="{StaticResource ModernTextBox}" Margin="0,0,0,10"/>

                            <TextBlock Text="Hasło" Foreground="#475569" FontWeight="Medium" Margin="0,0,0,4"/>
                            <PasswordBox x:Name="RegPasswordBox" Style="{StaticResource ModernPasswordBox}" Margin="0,0,0,10"/>

                            <TextBlock Text="Powtórz hasło" Foreground="#475569" FontWeight="Medium" Margin="0,0,0,4"/>
                            <PasswordBox x:Name="RegConfirmPasswordBox" Style="{StaticResource ModernPasswordBox}" Margin="0,0,0,15"/>

                            <Button Content="Utwórz konto" Style="{StaticResource PrimaryButton}" Click="Register_Click" Margin="0,0,0,12"/>

                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="Masz już konto? " Foreground="#64748b"/>
                                <Button Content="Zaloguj się" Style="{StaticResource LinkButton}" Click="SwitchToLogin_Click"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Status komunikatu dynamicznego przeniesiony do wnętrza kontenera -->
                        <Border x:Name="StatusBorder" CornerRadius="8" Padding="12,10" Margin="0,15,0,5" Visibility="Collapsed">
                            <TextBlock x:Name="StatusText" TextWrapping="Wrap" FontSize="13" FontWeight="Medium" HorizontalAlignment="Center"/>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>