<UserControl x:Class="DataAnalizer.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lucide="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="800"
             Background="#f8fafc">

    <UserControl.Resources>
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#f1f5f9"/>
            <Setter Property="BorderBrush" Value="#cbd5e1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#4f46e5"/>
                                <Setter TargetName="Border" Property="BorderThickness" Value="1.5"/>
                                <Setter Property="Background" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernPasswordBox" TargetType="PasswordBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#f1f5f9"/>
            <Setter Property="BorderBrush" Value="#cbd5e1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#4f46e5"/>
                                <Setter TargetName="Border" Property="BorderThickness" Value="1.5"/>
                                <Setter Property="Background" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#4f46e5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LinkButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#4f46e5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock Text="{TemplateBinding Content}" TextDecorations="Underline" VerticalAlignment="Center"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="EyeButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#94a3b8"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#4f46e5"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Border Background="White" CornerRadius="16" Width="450" VerticalAlignment="Center" Margin="20" MaxHeight="600">
            <Border.Effect>
                <DropShadowEffect BlurRadius="30" Color="#0f172a" Opacity="0.08" ShadowDepth="5"/>
            </Border.Effect>

            <Grid Margin="30,25">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,15">
                    <lucide:PackIconLucide Kind="Building2" Foreground="#4f46e5" Width="36" Height="36" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                    <TextBlock Text="{Binding TitleText}" FontSize="20" FontWeight="Bold" Foreground="#0f172a" HorizontalAlignment="Center"/>
                </StackPanel>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel>
                        
                        <StackPanel Visibility="{Binding LoginPanelVisibility}" KeyDown="LoginPanel_KeyDown">
                            <TextBlock Text="Nazwa użytkownika" Foreground="#475569" FontWeight="Medium" Margin="0,0,0,6"/>
                            <TextBox x:Name="UsernameBox" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" Margin="0,0,0,15"/>

                            <TextBlock Text="Hasło" Foreground="#475569" FontWeight="Medium" Margin="0,0,0,6"/>
                            <Grid Margin="0,0,0,10">
                                <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged">
                                    <PasswordBox.Style>
                                        <Style TargetType="PasswordBox" BasedOn="{StaticResource ModernPasswordBox}">
                                            <Setter Property="Padding" Value="10,5,35,5"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsPasswordVisible}" Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </PasswordBox.Style>
                                </PasswordBox>

                                <TextBox x:Name="PasswordTextBox" TextChanged="PasswordTextBox_TextChanged">
                                    <TextBox.Style>
                                        <Style TargetType="TextBox" BasedOn="{StaticResource ModernTextBox}">
                                            <Setter Property="Padding" Value="10,5,35,5"/>
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsPasswordVisible}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Style>
                                </TextBox>

                                <Button Command="{Binding TogglePasswordCommand}" Style="{StaticResource EyeButton}" HorizontalAlignment="Right" Margin="0,0,5,0">
                                    <Grid>
                                        <lucide:PackIconLucide Kind="Eye" Width="18" Height="18">
                                            <lucide:PackIconLucide.Style>
                                                <Style TargetType="lucide:PackIconLucide">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsPasswordVisible}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </lucide:PackIconLucide.Style>
                                        </lucide:PackIconLucide>
                                        <lucide:PackIconLucide Kind="EyeOff" Width="18" Height="18">
                                            <lucide:PackIconLucide.Style>
                                                <Style TargetType="lucide:PackIconLucide">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsPasswordVisible}" Value="True">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </lucide:PackIconLucide.Style>
                                        </lucide:PackIconLucide>
                                    </Grid>
                                </Button>
                            </Grid>

                            <CheckBox Content="Zapamiętaj mój login" IsChecked="{Binding RememberMe}" Foreground="#475569" Margin="2,0,0,15" Focusable="False"/>

                            <Button Content="Zaloguj się" Command="{Binding LoginCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,0,15"/>

                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="Nie masz konta? " Foreground="#64748b"/>
                                <Button Content="Zarejestruj się" Command="{Binding SwitchToRegisterCommand}" Style="{StaticResource LinkButton}" />
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Visibility="{Binding RegisterPanelVisibility}" KeyDown="RegisterPanel_KeyDown">
                            <TextBlock Text="Nazwa użytkownika" Foreground="#475569" FontWeight="Medium" Margin="0,0,0,4"/>
                            <TextBox x:Name="RegUsernameBox" Text="{Binding RegUsername, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" Margin="0,0,0,10"/>

                            <TextBlock Text="Adres E-mail" Foreground="#475569" FontWeight="Medium" Margin="0,0,0,4"/>
                            <TextBox x:Name="RegEmailBox" Text="{Binding RegEmail, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" Margin="0,0,0,10"/>

                            <TextBlock Text="Hasło" Foreground="#475569" FontWeight="Medium" Margin="0,0,0,4"/>
                            <Grid Margin="0,0,0,5">
                                <PasswordBox x:Name="RegPasswordBox" PasswordChanged="RegPasswordBox_PasswordChanged">
                                    <PasswordBox.Style>
                                        <Style TargetType="PasswordBox" BasedOn="{StaticResource ModernPasswordBox}">
                                            <Setter Property="Padding" Value="10,5,35,5"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsRegPasswordVisible}" Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </PasswordBox.Style>
                                </PasswordBox>

                                <TextBox x:Name="RegPasswordTextBox" TextChanged="RegPasswordTextBox_TextChanged">
                                    <TextBox.Style>
                                        <Style TargetType="TextBox" BasedOn="{StaticResource ModernTextBox}">
                                            <Setter Property="Padding" Value="10,5,35,5"/>
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsRegPasswordVisible}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Style>
                                </TextBox>

                                <Button Command="{Binding ToggleRegPasswordCommand}" Style="{StaticResource EyeButton}" HorizontalAlignment="Right" Margin="0,0,5,0">
                                    <Grid>
                                        <lucide:PackIconLucide Kind="Eye" Width="18" Height="18">
                                            <lucide:PackIconLucide.Style>
                                                <Style TargetType="lucide:PackIconLucide">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsRegPasswordVisible}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </lucide:PackIconLucide.Style>
                                        </lucide:PackIconLucide>
                                        <lucide:PackIconLucide Kind="EyeOff" Width="18" Height="18">
                                            <lucide:PackIconLucide.Style>
                                                <Style TargetType="lucide:PackIconLucide">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsRegPasswordVisible}" Value="True">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </lucide:PackIconLucide.Style>
                                        </lucide:PackIconLucide>
                                    </Grid>
                                </Button>
                            </Grid>

                            <Grid Visibility="{Binding PasswordStrengthVisibility}" Margin="2,0,0,10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <ProgressBar Value="{Binding PasswordStrengthValue}" Maximum="100" Height="4" Margin="0,0,0,4" 
                                             Foreground="{Binding PasswordStrengthColor}" Background="#e2e8f0" BorderThickness="0"/>
                                
                                <TextBlock Grid.Row="1" Text="{Binding PasswordStrengthLabel}" Foreground="{Binding PasswordStrengthColor}" FontSize="11" FontWeight="Medium"/>
                            </Grid>

                            <TextBlock Text="Powtórz hasło" Foreground="#475569" FontWeight="Medium" Margin="0,0,0,4"/>
                            <Grid Margin="0,0,0,15">
                                <PasswordBox x:Name="RegConfirmPasswordBox" PasswordChanged="RegConfirmPasswordBox_PasswordChanged">
                                    <PasswordBox.Style>
                                        <Style TargetType="PasswordBox" BasedOn="{StaticResource ModernPasswordBox}">
                                            <Setter Property="Padding" Value="10,5,35,5"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsRegConfirmPasswordVisible}" Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </PasswordBox.Style>
                                </PasswordBox>

                                <TextBox x:Name="RegConfirmPasswordTextBox" TextChanged="RegConfirmPasswordTextBox_TextChanged">
                                    <TextBox.Style>
                                        <Style TargetType="TextBox" BasedOn="{StaticResource ModernTextBox}">
                                            <Setter Property="Padding" Value="10,5,35,5"/>
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsRegConfirmPasswordVisible}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Style>
                                </TextBox>

                                <Button Command="{Binding ToggleRegConfirmPasswordCommand}" Style="{StaticResource EyeButton}" HorizontalAlignment="Right" Margin="0,0,5,0">
                                    <Grid>
                                        <lucide:PackIconLucide Kind="Eye" Width="18" Height="18">
                                            <lucide:PackIconLucide.Style>
                                                <Style TargetType="lucide:PackIconLucide">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsRegConfirmPasswordVisible}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </lucide:PackIconLucide.Style>
                                        </lucide:PackIconLucide>
                                        <lucide:PackIconLucide Kind="EyeOff" Width="18" Height="18">
                                            <lucide:PackIconLucide.Style>
                                                <Style TargetType="lucide:PackIconLucide">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsRegConfirmPasswordVisible}" Value="True">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </lucide:PackIconLucide.Style>
                                        </lucide:PackIconLucide>
                                    </Grid>
                                </Button>
                            </Grid>

                            <Button Content="Utwórz konto" Command="{Binding RegisterCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,0,12"/>

                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="Masz już konto? " Foreground="#64748b"/>
                                <Button Content="Zaloguj się" Command="{Binding SwitchToLoginCommand}" Style="{StaticResource LinkButton}"/>
                            </StackPanel>
                        </StackPanel>

                        <Border CornerRadius="8" Padding="12,10" Margin="0,15,0,5" Visibility="{Binding StatusVisibility}" Background="{Binding StatusBackground}">
                            <TextBlock Text="{Binding StatusText}" Foreground="{Binding StatusForeground}" TextWrapping="Wrap" FontSize="13" FontWeight="Medium" HorizontalAlignment="Center"/>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>