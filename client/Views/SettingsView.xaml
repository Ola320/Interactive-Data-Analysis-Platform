<UserControl x:Class="DataAnalizer.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Border Background="White" CornerRadius="15" Padding="30">
        <StackPanel>
            <TextBlock Text="Application Settings" FontSize="20" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,20"/>

            <TextBlock Text="Appearance" FontSize="16" FontWeight="SemiBold" Foreground="#64748B" Margin="0,10,0,10"/>
            <CheckBox Content="Enable Dark Mode" Margin="0,5" FontSize="14"/>
            <CheckBox Content="Compact UI Mode" Margin="0,5" FontSize="14"/>

            <Separator Margin="0,20" Background="#E2E8F0"/>

            <TextBlock Text="Data &amp; Privacy" FontSize="16" FontWeight="SemiBold" Foreground="#64748B" Margin="0,10,0,10"/>
            <CheckBox Content="Auto-save reports" IsChecked="True" Margin="0,5" FontSize="14"/>
            <StackPanel Orientation="Horizontal" Margin="0,10">
                <TextBlock Text="Export Format:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox Width="100">
                    <ComboBoxItem Content="CSV" IsSelected="True"/>
                    <ComboBoxItem Content="JSON"/>
                    <ComboBoxItem Content="PDF"/>
                </ComboBox>
            </StackPanel>

            <Button Content="Save All Changes" Width="150" Height="40" Background="#3B82F6" Foreground="White" 
                    BorderThickness="0" Margin="0,30,0,0" HorizontalAlignment="Left"/>
        </StackPanel>
    </Border>
</UserControl>