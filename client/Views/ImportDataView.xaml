<UserControl x:Class="DataAnalizer.Views.ImportDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Border Background="White" CornerRadius="15" Padding="30">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Import Your Data" FontSize="20" FontWeight="Bold" Foreground="#1E293B"/>
                <TextBlock Text="Select CSV or Excel files to begin analysis" Foreground="Gray" Margin="0,5,0,15"/>
                
                <Button Content="Choose Files" Width="120" Height="35" HorizontalAlignment="Left"
                        Background="#3B82F6" Foreground="White" BorderThickness="0"/>
            </StackPanel>

            <Border Grid.Row="1" Background="#F8FAFC" CornerRadius="10" BorderBrush="#E2E8F0" BorderThickness="1">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="No files selected yet" Foreground="#94A3B8" FontSize="14"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>